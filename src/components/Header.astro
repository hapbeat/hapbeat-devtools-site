---
/**
 * Header override.
 *
 * design の .topbar レイアウトを再現:
 *   [ brand | search | nav (Downloads / FAQ) | lang-switch | theme | github ]
 *
 * サイドバー / TOC は viewport 幅で自動レスポンシブ (components.css 参照)。
 * 開閉トグルは持たない。
 */
import SiteTitle from 'virtual:starlight/components/SiteTitle';
import Search from 'virtual:starlight/components/Search';
import SocialIcons from 'virtual:starlight/components/SocialIcons';
import ThemeSelect from 'virtual:starlight/components/ThemeSelect';
import LanguageSelect from 'virtual:starlight/components/LanguageSelect';

// メニュートグル (mobile) は Starlight 標準を PageFrame.astro が描画する。
// ここでは描画せず、CSS で position を上書きしてヘッダー左に再配置する
// (Starlight default は position:fixed で右端に固定)。
---

<div class="hb-topbar">
  <div class="hb-topbar__left">
    <SiteTitle {...Astro.props} />
  </div>

  <div class="hb-topbar__center">
    <Search {...Astro.props} />
  </div>

  <div class="hb-topbar__right">
    <nav class="hb-topbar__nav" aria-label="主要セクション">
      <a href="/docs/getting-started/">Docs</a>
      <a href="/downloads/">Downloads</a>
      <a href="/faq/">FAQ</a>
    </nav>
    <LanguageSelect {...Astro.props} />
    <div class="hb-topbar__icons">
      <ThemeSelect {...Astro.props} />
      <SocialIcons {...Astro.props} />
    </div>
  </div>
</div>

<style>
  .hb-topbar {
    display: grid;
    grid-template-columns: auto 1fr auto;
    grid-template-areas: 'left center right';
    align-items: center;
    gap: 32px;
    width: 100%;
    padding: 0 8px;
  }
  .hb-topbar__left {
    grid-area: left;
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .hb-topbar__center {
    grid-area: center;
    display: flex;
    justify-content: center;
    min-width: 0;
  }
  .hb-topbar__right {
    grid-area: right;
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .hb-topbar__menu {
    grid-area: menu;
    display: none;
  }
  .hb-topbar__nav {
    display: inline-flex;
    align-items: center;
    gap: 6px;
  }
  .hb-topbar__nav a {
    height: 32px;
    padding: 0 12px;
    border-radius: 7px;
    font-size: 13px;
    color: var(--hb-ink-mute);
    display: inline-flex;
    align-items: center;
    text-decoration: none !important;
    transition: background 0.15s, color 0.15s;
  }
  .hb-topbar__nav a:hover {
    color: var(--hb-ink);
    background: var(--hb-bg-soft);
  }
  .hb-topbar__nav a[aria-current='page'] {
    color: var(--hb-ink);
    background: var(--hb-bg-soft);
  }
  .hb-topbar__icons {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding-left: 8px;
    border-left: 1px solid var(--hb-line);
    margin-left: 4px;
  }

  /* タブレット以下: nav の text label は隠して icons + lang のみ */
  @media (max-width: 60rem) {
    .hb-topbar {
      gap: 20px;
    }
    .hb-topbar__nav {
      display: none;
    }
  }

  /* モバイル: search 隠して brand + right。
     PageFrame.astro が描画する MobileMenuToggle は CSS で
     ヘッダー左に再配置する (components.css 側)。
     ハンバーガー領域確保 (padding-left) は docs ページ
     (sidebar あり) のみに適用する。 */
  @media (max-width: 50rem) {
    .hb-topbar {
      grid-template-columns: auto 1fr auto;
      grid-template-areas: 'left center right';
      gap: 12px;
    }
    :global(html[data-has-sidebar]) .hb-topbar {
      padding-left: 56px;
    }
    .hb-topbar__center {
      display: none;
    }
    .hb-topbar__icons {
      border-left: 0;
      padding-left: 0;
      margin-left: 0;
    }
  }
</style>
